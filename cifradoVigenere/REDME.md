# How to compile and run the C program

1. **Execute the following command to compile the program:**
   ```bash
   gcc cifradoVigenere.c -o cifradoVigenere
   ```

2. **Execute the following command to run the program:**
   ```bash
   ./cifradoVigenere <key> <inputfile.txt>
   ```
